{
  "name": "budget-manager",
  "private": true,
  "scripts": {
    "pre-commit": "lint-staged --allow-empty",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0"
  },
  "lint-staged": {
    "*.{js,ts,java,md,yml,yaml,json}": "npx prettier --write"
  }
}
